/**
 * Dropdowns
 * 
 * Types:
 *  1. with <select> tag
 *  2. with any other tag
 */
// <select> tag dropdowns
/**
 *      option-tags are present index-wise in the select-tag
 *      option-tags has value-attribute
 *      option-tags has text-value
 *      text-value of the option-tag is getting displayed to the user
 */

// How to interact with <select> tag dropdowns
/**
 * 1. Find the locator of select-tag
 * 2. using the above locator, find the webElement
 * 3. using specific function select data in dropdown
 *      selectByVisibleText - selects the data based on the text-value of option-tag
 *      selectByAttribute - selects the data based on the value-attribute of option-tag
 *      selectByIndex -  - selects the data based on the index of option-tag
 * 
 * 
 */